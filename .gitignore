# Bỏ qua thư mục chứa các package đã cài (rất nặng, không cần push)
node_modules/
# Bỏ file môi trường (chứa key, token… không được public)
.env
# Nhưng giữ file mẫu để mọi người biết cần tạo biến gì
!.env.example
# Cấu hình JS (không bắt buộc bỏ nhưng thường không cần push)
jsconfig.json
# --- REACT ---
# Thư mục và file log (log lỗi, log chạy server) — không cần đẩy lên Git
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
# Các folder build (tự tạo khi build dự án, không push)
node_modules
dist
dist-ssr
*.local
# --- FILE CỦA EDITOR (VSCode, IDEA...) ---
# VSCode folder — chứa config cá nhân (không cần đẩy lên Git)
.vscode/*
# Nhưng giữ extensions.json (đề xuất extension cho dự án)
!.vscode/extensions.json


# Các folder của IDE khác
.idea
# File hệ thống của MacOS
.DS_Store


# File hệ thống của Windows / Visual Studio
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
